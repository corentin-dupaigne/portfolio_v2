replicaCount: 1

image:
  repository: "ghcr.io/corentin-dupaigne/portfolio-v2/portfolio-v2"
  # Managed by .github/workflows/build-and-release.yml — every push to main
  # rewrites this to the commit SHA, which is what triggers the Argo CD sync.
  # Do not set this back to a mutable tag.
  tag: "379ed4631628d0541ff35986ba0e15a0e46dae21"

resources:
  requests:
    cpu: 10m
    memory: 16Mi
  limits:
    memory: 64Mi

hostname: "corentindupaigne.com"

gateway:
  name: gateway
  namespace: default

servicePort: 80
containerPort: 80
